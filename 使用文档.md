# 暖愈心伴（Warm Mate）使用文档

> 一款基于 **DeepSeek 大模型** 的智能心理健康服务平台 —— 「预防为先 · 主动关怀」的随身心理陪伴。

- **版本**：v1.1.2
- **前端**：跨端 H5（UniApp 风格，Capacitor 打包 Android 应用）
- **后端**：Python FastAPI + DeepSeek（deepseek-chat）
- **服务部署**：阿里云 ECS（Ubuntu 22.04，Docker/systemd）

---

## 一、项目简介

「暖愈心伴」整合 **AI 智能对话、专业心理测评、健康科普、咨询预约** 四大核心功能，构建"预防 - 评估 - 支持 - 干预"全链条心理健康服务体系：

- **AI 暖心对话**：基于 DeepSeek 大模型，24 小时在线心理陪伴（"小暖"人设，支持上下文、打字机逐字显示）。
- **专业心理测评**：集成 PHQ-9、GAD-7、CPSS、UCLA 等国际权威量表，自动计分、生成报告与建议。
- **健康科普**：涵盖心理健康、压力管理、睡眠健康、营养健康、运动健身、疾病预防六大类，支持分类筛选与搜索。
- **专业资源预约演示**：提供6类校内外资源入口与预约流程示例；不代表已签约咨询师、机构或真实服务。
- **个人中心**：对话历史、测评记录、预约记录、收藏、设置等。

---

## 二、功能说明

### 2.1 登录 / 注册
- 手机号 + 密码登录，注册需二次确认密码。
- 支持密码明文/密文切换（👁）。
- 支持"忘记密码"说明入口。
- 登录/注册前需勾选《用户协议》《隐私政策》。
- 演示账号：手机号 `13800138000`，密码任意 ≥6 位。

### 2.2 AI 暖心对话
- 底部/首页进入"AI 暖心对话"，可随时倾诉。
- 输入内容后，小暖会以打字机效果逐字回复。
- 支持上下文记忆，连续对话更连贯。
- 支持快捷提问（如"最近压力大"、"总是焦虑怎么办"等）。
- 断网时自动回退到内置共情回复，保证离线可演示。

### 2.3 心理测评
- 4 种权威量表：PHQ-9（抑郁）、GAD-7（焦虑）、CPSS（压力）、UCLA（孤独感）。
- 按类别筛选（情绪状态 / 压力评估 / 人际关系 / 睡眠质量）。
- 单量表答题，完成后自动计分，生成等级、描述与建议。
- 测评结果提示："本测评仅供自我了解参考，不构成医学诊断"。

### 2.4 健康科普
- 分类浏览 + 关键词搜索。
- 文章详情支持收藏、稍后读。
- 「我的收藏」中可查看收藏的文章。

### 2.5 咨询师预约
- 浏览专业资源入口示例（资源类型、适用场景、接入状态）。
- 按专业领域筛选。
- 填写预约表单（姓名、手机号、性别、年龄、内容描述、紧急程度、时间偏好）。
- 提交后仅生成本次会话的演示记录，不发送至真实机构。

### 2.6 个人中心（我的）
- 查看统计（对话次数、测评报告、咨询预约）。
- 历史记录（对话 / 测评 / 预约），无数据时显示"暂无 XX 记录"。
- 我的收藏、设置（版本信息、AI 引擎）。

---

## 三、下载与安装

### 3.1 Android APK
从以下地址下载最新版 APK：

```
http://47.93.117.13:8080/WarmMate.apk
```

**安装步骤**：
1. 手机「设置 → 应用管理」卸载旧版「暖愈心伴」（若已安装）。
2. 手机浏览器打开上面地址，下载 APK（约 5 MB）。
3. 点击 APK 安装；如提示"允许安装未知来源应用"，按提示开启。
4. 打开「暖愈心伴」→ 输入手机号 `13800138000` + 任意 ≥6 位密码 → 登录。

**安装要求**：Android 7.0 及以上，需联网（AI 对话依赖云端后端）。

### 3.2 网页版
浏览器直接访问：

```
http://47.93.117.13:8080/
```

---

## 四、技术架构

```
┌─────────────────────────────────────────────┐
│  前端（本目录 prototype/）                    │
│  UniApp 风格 H5 · Capacitor 打包 Android     │
└──────────────────────┬──────────────────────┘
                       │ HTTPS/HTTP (REST)
┌──────────────────────▼──────────────────────┐
│  后端 FastAPI（阿里云 ECS :8080）             │
│  /api/chat  /api/health                      │
│  CORS 跨域 · 前台托管                        │
└──────────────────────┬──────────────────────┘
                       │ OpenAI 兼容 API
┌──────────────────────▼──────────────────────┐
│  DeepSeek 大模型（deepseek-chat）             │
│  "小暖"心理陪伴师人设                         │
└─────────────────────────────────────────────┘
```

### 4.1 前端
- `index.html`：页面结构（登录、首页、对话、测评、科普、预约、我的）。
- `css/style.css`：暖色系视觉规范与组件样式。
- `js/data.js`：模拟数据（量表、科普、咨询师、共情回复）。
- `js/app.js`：路由、交互逻辑、后端调用。

### 4.2 后端（`backend/main.py`）
- **`GET /api/health`**：健康检查。
- **`POST /api/chat`**：AI 对话，入参 `{ message, history }`，返回 `{ reply, model }`。
- 系统提示词将 DeepSeek 设定为明确披露AI身份的心理支持助手；模型前增加确定性危机安全门。
- CORS：`allow_origins=["*"]`，支持跨域。

### 4.3 DeepSeek 配置
- 模型：`deepseek-chat`。
- 参数：`temperature=0.8`、`max_tokens=600`。
- API Key 通过服务器环境变量 `DEEPSEEK_API_KEY` 注入，**不写入前端与代码**。

---

## 五、后端接口说明

### `GET /api/health`
```json
{ "status": "ok", "app": "warm-mate", "version": "1.1.2", "ai": "deepseek", "model": "deepseek-chat", "key_configured": true }
```

### `POST /api/chat`
请求体：
```json
{ "message": "最近压力很大", "history": [ { "role": "user", "content": "你好" }, { "role": "assistant", "content": "你好呀，我是小暖 🌱" } ] }
```
响应：
```json
{ "reply": "嗯，听起来你最近确实承受了不小压力……", "model": "deepseek-chat" }
```

> history 为可选的对话上下文（user/assistant 交替），用于多轮记忆。

---

## 六、部署说明（云端）

### 6.1 后端
```bash
# 代码位于 /opt/warm-mate/
cd /opt/warm-mate
# 虚拟环境已就绪（venv）
# systemd 服务已配置
systemctl status warm-mate        # 查看状态
systemctl restart warm-mate       # 重启
journalctl -u warm-mate -n 50     # 查看日志
```
- 监听 `0.0.0.0:8080`，开机自启、崩溃自动重启。
- 环境变量：`DEEPSEEK_API_KEY`（在 `warm-mate.service` 中）。

### 6.2 前端
- 静态文件由后端托管于 `/opt/warm-mate/static/`。
- 前端通过 `js/app.js` 中的 `CLOUD_API` 指向后端地址。
- 公网访问：`http://47.93.117.13:8080/`。

### 6.3 数据库
- 当前测评/预约等数据暂存于前端内存（未持久化）。
- 如需持久化，后续可接入 MySQL/PostgreSQL。

---

## 七、目录结构

```
warm-mate-plus/
├── prototype/            # 前端源码
│   ├── index.html
│   ├── css/style.css
│   └── js/
│       ├── data.js       # 模拟数据
│       └── app.js        # 逻辑与路由
├── backend/              # 后端源码
│   ├── main.py           # FastAPI 应用
│   └── requirements.txt
├── app/                  # Capacitor 工程（Android 打包）
│   ├── www/              # 前端构建产物
│   ├── android/          # Android 原生工程
│   └── assets/           # 应用图标源图
├── dist/                 # 打包产物
│   └── WarmMate-v1.1.2.apk
└── WORKBENCH.md          # 阿里云 ECS 连接信息
```

---

## 八、常见问题（FAQ）

**Q1：AI 对话没反应？**
- 确认手机已联网，且能访问 `http://47.93.117.13:8080`。
- 首次请求可能需要几秒（DeepSeek 生成中），顶部会显示"正在输入"。
- 若网络异常，会自动回退到内置共情回复（离线兜底）。

**Q2：历史记录为空？**
- 当前数据存于前端内存，每次打开 App 重新开始。
- 没有任何记录时，会显示"暂无对话/测评/预约记录"。

**Q3：测评结果算不算诊断？**
- 不算。结果显示"仅供自我了解参考，不构成医学诊断"。
- 明显异常时建议预约专业咨询或就医。

**Q4：如何重新安装/升级？**
- 先卸载「暖愈心伴」，再从 `http://47.93.117.13:8080/WarmMate.apk` 下载安装。
- 避免同名文件被缓存：直接用教程中的完整地址。

**Q5：遇到页面显示异常？**
- 多为设备 WebView 兼容性导致，可尝试升级系统 WebView 或用更高版本 Android 设备。

---

## 九、版本记录

| 版本 | 说明 |
|---|---|
| v1.0.0 | 初版，构建 App 骨架与基础功能 |
| v1.1.2 | DeepSeek 接入、跨端打包、修复页面跳转与返回键问题、清理调试信息、最终可交付版 |

---

## 十、重要声明

- 本项目为心理健康支持工具，**不构成医学诊断或治疗建议**。
- AI 对话内容由 DeepSeek 生成，仅供参考；存在立即危险时请直接联系 **110、120、最近急诊和身边可信任的人**。
- 当前为功能原型，尚未形成生产级账户、持久化、导出删除与加密体系，请勿输入可识别的敏感信息。

---

*暖愈心伴 Warm Mate · 「预防为先 · 主动关怀」*
